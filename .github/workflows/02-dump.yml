name: 02 DUMP
on:
  push: 
jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ toJSON(github) }}"
      - run: echo "${{ toJSON(runner) }}"
      - run: echo "${{ toJSON(job) }}"

# other objects: strategy, matrix
